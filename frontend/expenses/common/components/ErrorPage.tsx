import React, {ReactElement} from "react";


type Props = {
    prompt: string;
    advice?: string;
    nextRoute?: string;
}
const ErrorPage: React.FC = ({ prompt, advice, nextRoute}: Props): ReactElement => (
    <>
        <
    </>
)
